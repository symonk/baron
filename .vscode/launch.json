{
    "configurations": [
        {
            "cwd": "${workspaceFolder}",
            "name": "Launch Program",
            "program": "${workspaceFolder}//cmd//baron//main.go",
            "request": "launch",
            "type": "go"
        }
    ],
    "version": "0.2.0"
}
